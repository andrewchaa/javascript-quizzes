{
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "jest": "^29.4.3",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint",
    "test": "jest"
  }
}
